{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "@types/js-yaml": "^4.0.5",
    "hardhat": "^2.11.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network $NETWORK",
    "verify": "npx hardhat verify --network $NETWORK $CONTRACT_ADDRESS DPM DPM"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.3",
    "js-yaml": "^4.1.0"
  }
}
